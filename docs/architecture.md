# Architecture documentation moved

The current architecture is maintained in [ARCHITECTURE.md](../ARCHITECTURE.md).

That document covers the committed Canvas CRDT V2 model, Surface Shadow and receipt system, sidecar/GUID/epoch identity, file-operation boundaries, session/link state, relay topology, and real-Obsidian testing architecture.

Historical architecture and redesign documents remain under `workflowArtifacts/` as engineering evidence. They are not current product documentation.
